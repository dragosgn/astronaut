version: 0.1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    build:
      commands: ['npm build']
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
